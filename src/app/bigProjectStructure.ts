export interface BigProjectStructure {
    id: number;
    image: string;
    imageAlt: string;
    title: string;
    description: string;
    skills: Array<string>;
    githubUrl: string;
    siteUrl: string;    
  }